using Algorithms;
using DataStructures.Lists;
using System;

namespace ForDebug
{
    class Program
    {
        static void Main(string[] args)
        {
            var p = new Stack(5);

            p.Push(10);
            p.Push(20);
            p.Push(30);

            Console.ReadLine();
        }
    }
}
